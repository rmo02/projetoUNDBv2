import { Center, Text, VStack } from "native-base";

export function Login () {
    return (
        <VStack flex={1} bg='fundo'>
            <Center>
                <Text>Login</Text>
            </Center>
        </VStack>
    );
}